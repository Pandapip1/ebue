# SPDX-FileCopyrightText: (C) 2026 Gavin John
# SPDX-License-Identifier: GPL-3.0-or-later
#
# The number of functions that a public header declares, that this
# library implements, and that no test/*.c references.  Read by
# tools/lint-unreferenced.sh, which fails if the real count is higher
# and prints a nudge if it is lower.  See that script's header for what
# "references" means, and why the ratchet is one-way.
#
# This is not a target.  It is a number that has only ever gone down --
# 165 when test/verification-measures.md measured it at 082ed2c, before
# the POSIX clause audits landed -- and the point of committing it is
# that the next rise is a failure that names the function, instead of a
# fact somebody rediscovers in an audit a year later.
#
# The full list is written to obj/lint/unreferenced.txt on every run.
#
49
