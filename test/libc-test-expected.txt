# SPDX-FileCopyrightText: (C) 2026 Gavin John
# SPDX-License-Identifier: GPL-3.0-or-later
#
# test/libc-test-expected.txt -- the adjudication ledger for
# third_party/libc-test, read by tools/libc-test.sh.
#
# One or more profile rules per corpus test:
#
#     <name>  <profile selector>  <disposition>  <reason>
#
# The dispositions are PASS, BUG, UNIMPL, NA and FLAKY.  Every non-PASS
# row requires a reason.  Compiler/process observations remain separate:
# pedantic requires BUG to build and fail and UNIMPL not to build; strict
# performs those probes and then rejects either disposition.
#
# A disposition may be overridden by adding a more specific profile rule.
# Profile dimensions include runtime, target and host architecture, WOW64,
# kernel32 support and named runtime capabilities. The shared resolver
# rejects ambiguous matches and profiles for which no rule exists. This
# baseline deliberately claims only the configuration in which it was measured;
# another platform must be annotated before it can use the ledger.
#
# This file does NOT need one row per real case. It lists only the cases
# whose disposition deviates from PASS, plus one trailing wildcard row
# (case "*") that declares PASS as this corpus's own default for every
# case with no more specific row. That row is not decoration:
# tools/testlib.py's resolve() falls back to a case="*" rule whenever a
# case-specific search comes up empty, so "everything else is PASS" is a
# real, matched rule here, not a hidden default in Python or in
# tools/libc-test.sh. Which cases are "real" -- so that a case with no row
# here still gets adjudicated, rather than silently dropped -- comes
# independently from tools/libc-test.sh's own glob over
# third_party/libc-test/src/{functional,regression}/*.c (see its
# read_ledger()), not from what happens to be listed below. This mirrors
# test/posix-opts-expected.txt's own wildcard row exactly (see commit
# 93bbaa3, "test: default posix-opts cases to PASS via an explicit
# wildcard row"); this ledger previously took the opposite position --
# every real case needed an explicit row, and an absent one was a hard
# error -- until the project decided that PASS-by-default is fine here
# too, as long as the default is a real, visible row rather than a
# hidden one.
#
# Baseline: ntlibc 661c577, libc-test 68edb8b, x86_64-win32-tcc, under
# the project's patched Wine (which implements RtlCloneUserProcess, so
# nothing here is Wine-fork-blocked; on stock Wine several of these
# become NA at run time instead, by design).
#
# COUNTS PASS=102 BUG=3 UNIMPL=26 NA=14 FLAKY=1
# (whole-corpus totals once the wildcard default below is applied to
# every real case tools/libc-test.sh discovers -- not a count of rows in
# this file, which lists only the 45 explicit exceptions plus the
# wildcard.)
clocale_mbfuncs                   runtime=wine,target_arch=x86_64,kernel32=no  NA  design difference, not a defect: it opens with setlocale(LC_CTYPE, "C") and then requires MB_CUR_MAX == 1, i.e. a single-byte C locale. UTF-8 is this library's only encoding (src/internal/utf.c), so MB_CUR_MAX is 4 in every locale and test/posix-msgcat.c:test_codeset_is_utf8_and_the_converter_exists asserts that live. Every one of the 8437 failure lines descends from that one premise -- the byte-to-wchar_t map, the wctob round trip and the non-image scan all assume 256 single-byte characters. Built and ran here only as of include/langinfo.h; it was UNIMPL for want of that header
crypt                             runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  crypt.c:2: error: include file 'crypt.h' not found
daemon-failure                    runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  tcc: error: unresolved reference to 'daemon'
dlopen                            runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  tcc: error: unresolved reference to '__rpath'
dlopen_dso                        runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  tcc: error: unresolved reference to 'main'
dn_expand-empty                   runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  dn_expand-empty.c:4: error: include file 'resolv.h' not found
dn_expand-ptr-0                   runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  dn_expand-ptr-0.c:4: error: include file 'resolv.h' not found
env                               runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  tcc: error: unresolved reference to 'clearenv' (dropped as an unused non-POSIX extension in 49b8099)
execle-env                        runtime=wine,target_arch=x86_64,kernel32=no  NA  no /bin/sh on the target; execle("/bin/sh",...) cannot succeed
fcntl                             runtime=wine,target_arch=x86_64,kernel32=no  BUG  F_SETLK across fork(): the child inherits the same NT file object, so its byte-range-lock request does not conflict with the parent's lock as POSIX requires for the child's distinct process; the test reports "lock failed to work"
flockfile-list                    runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  lib/libc.a: error: link symbol 'malloc' defined twice
ipc_msg                           runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  ipc_msg.c:9: error: include file 'sys/msg.h' not found
ipc_sem                           runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  ipc_sem.c:9: error: include file 'sys/sem.h' not found
ipc_shm                           runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  ipc_shm.c:9: error: include file 'sys/shm.h' not found
malloc-brk-fail                   runtime=wine,target_arch=x86_64,kernel32=no  NA  design difference, not a defect: t_vmfill now genuinely exhausts free virtual address space, but malloc() uses the pre-existing NT process heap (src/malloc/nt/plat_malloc.c), whose already-reserved arena can still satisfy malloc(10000); exhausting addresses therefore does not establish this test's musl/brk-specific premise that the next allocation must fail
malloc-oom                        runtime=wine,target_arch=x86_64,kernel32=no  NA  upstream t_memfill reaches real virtual-address exhaustion, then declines the experiment because t_setrlim(RLIMIT_DATA, 0) is stubbed: setrlimit() is not enforced on this target, so the helper cannot exhaust the NT process heap's existing reserve and the required malloc-OOM premise is not established
mbc                               runtime=wine,target_arch=x86_64,kernel32=no  NA  design difference, not a defect: its one failure is mbrtowc("\xf0\x90\x80\x80") yielding 0xd800 where it wants 0x10000. wchar_t is 16-bit on this target because that is the Windows ABI (arch/x86_64/bits/alltypes.h.in), so a non-BMP character is a UTF-16 surrogate pair and cannot be one wchar_t; src/stdlib/mbrtowc.c returns the high surrogate and holds the low one in the mbstate_t, and test/posix-wchar.c:posix_wchar_wcwidth_non_bmp records the same constraint. Built and ran here only as of include/langinfo.h
mntent                            runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  mntent.c:5: error: include file 'mntent.h' not found
popen                             runtime=wine,target_arch=x86_64,kernel32=no  NA  no /bin/sh on the target -- the shell script reaches cmd.exe, which rejects it (external-suites.md, build assumption 3)
pthread_atfork-errno-clobber      runtime=wine,target_arch=x86_64,kernel32=no  NA      t_setrlim exits 77 because setrlimit() is not enforced on this target, so the errno-preservation premise cannot be exercised
pthread_cancel-points             runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  pthread_cancel-points.c:3: error: include file 'pthread.h' not found
pthread_create-oom                runtime=wine,target_arch=x86_64,kernel32=no  NA  upstream t_memfill reaches real virtual-address exhaustion, then declines because t_setrlim(RLIMIT_DATA, 0) is not enforced; without exhausting the NT process heap's existing reserve it cannot establish the case's required pthread_create-OOM premise
pthread_exit-dtor                 runtime=wine,target_arch=x86_64,kernel32=no  FLAKY  patched Wine's concurrent RtlCloneUserProcess path can stall: measured 40/40 serial passes but 2 timeouts in 160 runs at 20-way concurrency; no destructor assertion failed. Re-reviewed ntlibc's own fork() call site (src/process/fork.c): the handle-inheritance marking/unmarking around RtlCloneUserProcess and the __children table bookkeeping are correct on inspection and introduce no synchronization ntlibc itself controls that could explain a concurrency-only stall, consistent with attributing the stall to the patched Wine fork's own concurrent clone path rather than to this library. Could not refresh the ratio here: this sandbox's Wine cannot execute any x86_64 PE binary at all (stock aarch64 Wine has no x86_64 support, and Wine run under qemu-user's x86_64 ELF emulation crashes inside Wine's own PE loader during guest-address-space setup, before any PE code runs), and building the project's patched Wine fork from source in-sandbox was not sustainable given this host's shared, limited disk and CPU. The 40/40 serial, 2/160-at-20-way numbers above remain the best available evidence
pthread_mutex                     runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  pthread_mutex.c:2: error: include file 'pthread.h' not found
pthread_mutex_pi                  runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  pthread_mutex_pi.c:2: error: include file 'pthread.h' not found
pthread_once-deadlock             runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  pthread_once-deadlock.c:3: error: include file 'pthread.h' not found
pthread_robust                    runtime=wine,target_arch=x86_64,kernel32=no  BUG     owner death, EOWNERDEAD recovery, consistency, and ENOTRECOVERABLE pass; the remaining case destroys a mutex while still locked, for which ntlibc deliberately returns EBUSY
setenv-oom                        runtime=wine,target_arch=x86_64,kernel32=no  NA  upstream t_memfill reaches real virtual-address exhaustion, then declines because t_setrlim(RLIMIT_DATA, 0) is not enforced; the NT process heap can retain already-reserved space, so setenv() succeeding would not be an OOM defect without that missing premise
sigprocmask-internal              runtime=wine,target_arch=x86_64,kernel32=no  NA  design difference, not a defect: __libc_current_sigrtmin() is 35 here, so signals 32-34 are not reserved and are blockable
socket                            runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  socket.c:38: error: 'SOCK_CLOEXEC' undeclared
spawn                             runtime=wine,target_arch=x86_64,kernel32=no  NA  <spawn.h> now exists and everything in this test but one line passes; it spawns the program "echo", and NT has no echo executable -- echo is a cmd.exe builtin, not an image on PATH, so posix_spawnp() answers ENOENT (2) and the waitpid()/read() lines cascade from it. src/process/find_program.c searches PATH for a real image, which is what POSIX specifies. ntlibc's own posix_spawnp() coverage is test/posix-spawn.c test_spawnp_path_search, which resolves a name that does exist
sscanf_long                       runtime=wine,target_arch=x86_64,kernel32=no  NA  NOT-YET-TRIAGED -- unverifiable in this environment (t_setrlim stubbed; the test needs an enforced RLIMIT_STACK), so it never reaches adjudication here.
syscall-sign-extend               runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  syscall-sign-extend.c:7: error: include file 'sys/syscall.h' not found
tgmath                            runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  tgmath.c:1: error: include file 'tgmath.h' not found
tls_align                         runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  tcc: error: unresolved reference to 't'
tls_align_dlopen                  runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  tcc: error: unresolved reference to '__rpath'
tls_align_dso                     runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  tcc: error: unresolved reference to 'main'
tls_get_new-dtv                   runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  tls_get_new-dtv.c:2: error: include file 'pthread.h' not found
tls_get_new-dtv_dso               runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  tcc: error: unresolved reference to 'main'
tls_init_dlopen                   runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  tcc: error: unresolved reference to '__rpath'
tls_init_dso                      runtime=wine,target_arch=x86_64,kernel32=no  UNIMPL  tcc: error: unresolved reference to 'main'
tls_local_exec                    runtime=wine,target_arch=x86_64,kernel32=no  BUG  the test now builds and its initialized/zero TLS values survive on both threads, but every over-aligned address fails: pinned tcc writes IMAGE_TLS_DIRECTORY.Characteristics=0, so the PE loader is never told the 4096-byte alignment required by the input TLS section
vfork                             runtime=wine,target_arch=x86_64,kernel32=no  NA  no /bin/sh on the target; the second half execl()s "/bin/sh -c"
wordexp                           runtime=wine,target_arch=x86_64,kernel32=no  NA  the former BUGs (IFS splitting and ${x:-d}/${x-d}/${x:+y}) pass; the remaining full-corpus cases assume a complete external POSIX shell environment, including unsupported case grammar and utility images such as echo/printf that this NT target does not provide; the supported wordexp subset is covered in-tree by test/posix-glob.c
*	*	PASS	no more specific rule matched; suite defaults to PASS
