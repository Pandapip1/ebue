<!--
SPDX-FileCopyrightText: (C) 2026 Gavin John
SPDX-License-Identifier: GPL-3.0-or-later

GENERATED FILE -- do not edit by hand.
Regenerate with:  tools/libc-test-map.sh
Verified in the gate by:  tools/libc-test-map.sh --check
-->

# `libc-test` coverage map

| | |
|---|---|
| ntlibc | `0d9eed249b7cdfc3af78cbf736d6884a891c0c94` |
| libc-test | `68edb8bd73dab8147ee54c8bec638f4d2b3cff37` |
| generated by | `tools/libc-test-map.sh` |

**This file is checked in on purpose.** The value of a gap report is in
its **diff**: `git diff test/LIBC-TEST-MAP.generated.md` after landing
`pthread.h` would show 16 tests move out of *blocked* and 7 more
re-classify onto the next header they need, and that diff is
a better changelog entry than any prose. It also means the report cannot
rot silently — a stale checked-in file is visible in review; a CI
artifact nobody downloaded is not.

`tools/libc-test.sh` ends its summary with a bare count of unbuildable
tests. This file is that count taken apart.

## 1. Scope, and the honest denominator

`tools/gate.sh` runs **functional + regression only**. Two fractions
follow from that, and the smaller one is the true one:

| denominator | files | adjudicated | share |
|---|---|---|---|
| what the gate opted into (`src/functional` + `src/regression`) | 146 | 146 | 100% |
| **upstream's whole test corpus** | 425 | 146 | **34.4%** |

The gap between those two rows is almost entirely `src/math`:
**199 files**, on disk in the submodule, never built by
default. Also excluded: `src/api` (79 compile-only
declaration checks) and `src/musl` (1 file, `pleval`).
By other measures `src/math` is a larger share still — 679 of the 933
files under `src/`, and 9.6 MB of 10.7 MB.

So the headline is not "we run 62 of 146". It is:

> **Of the 425 test files musl's `libc-test` ships, this
> tree adjudicates 146 (34.4%), builds 89
> (20.9% of upstream), and gets a real behavioural
> verdict from fewer still.**

`tools/libc-test.sh math` exits 2 without `LIBC_TEST_MATH=`, which is
the right behaviour — a mode that quietly succeeded having run nothing
would be worse than one that is missing. But "correctly refuses to run"
is not "accounted for", and `test/external-suites.md` already records
three untriaged findings in there that nothing in this tree tracks:

- **`fma()` is not fused.** `src/math/sanity/fma.h` cases fail at
  0.5–0.8 ULP. A correct `fma` is exact — 0 ULP by definition — so any
  nonzero ULP is a categorical failure, not a tolerance question. This
  is the most concrete of the three.
- **Spurious `FE_DIVBYZERO`** from `logb(±inf)`, `exp(inf)`, and
  from `pow` on ordinary finite arguments. POSIX specifies no exception
  for any of them.
- **The `isless` family raises `FE_INVALID` on NaN**, specified not to.

None of those is visible anywhere in the gate. They are stated here so
that this file's denominator cannot be read as an all-clear for the 82
of 174 linkable math tests that fail.

## 2. The A/B/C split

| class | tests | share of 146 | what it means |
|---|---|---|---|
| **A — header absent** | 40 | 27.4% | the `#include` fails. The honest failure: we do not claim the interface and a portable program finds out at compile time. |
| **B — header present, interface missing** | 17 | 11.6% | the `#include` **succeeds**, and it dies at link. The worst of the three for a consumer, and the one the function-interface ledger cannot see. |
| **C — builds** | 89 | 61.0% | reaches `tools/libc-test.sh`'s adjudication at all |

`A + B + C = 40 + 17 + 89 = 146`, and the census is 146.
(Invariant 2, below.)

## 3. Header levers — what to build next

Tests naming each absent header (a test may name several), and how many
would stop being blocked if that header **alone** arrived:

| header | tests naming it | unblocked by it **alone** | absent interfaces | tests per interface |
|---|---|---|---|---|
| `pthread.h` | 23 | 16 | 70 | **0.33** |
| `semaphore.h` | 9 | 2 | 10 | **0.90** |
| `langinfo.h` | 3 | 3 | 2 | **1.50** |
| `resolv.h` | 2 | 2 | — | — |
| `iconv.h` | 2 | 2 | 3 | **0.67** |
| `sys/mman.h` | 2 | 1 | 3 | **0.67** |
| `tgmath.h` | 1 | 1 | — | — |
| `sys/syscall.h` | 1 | 1 | — | — |
| `sys/shm.h` | 1 | 1 | 4 | **0.25** |
| `sys/sem.h` | 1 | 1 | 3 | **0.33** |
| `sys/msg.h` | 1 | 1 | 4 | **0.25** |
| `mntent.h` | 1 | 1 | — | — |
| `crypt.h` | 1 | 1 | — | — |

Greedy closure — add headers best-first, and watch the residue:

```
start                        blocked on an absent header:  40
+pthread.h                  unblocks  16    still blocked:  24
+semaphore.h                unblocks   8    still blocked:  16
+langinfo.h                 unblocks   3    still blocked:  13
+iconv.h                    unblocks   2    still blocked:  11
+resolv.h                   unblocks   2    still blocked:   9
+sys/mman.h                 unblocks   2    still blocked:   7
+crypt.h                    unblocks   1    still blocked:   6
+mntent.h                   unblocks   1    still blocked:   5
+sys/msg.h                  unblocks   1    still blocked:   4
+sys/sem.h                  unblocks   1    still blocked:   3
+sys/shm.h                  unblocks   1    still blocked:   2
+sys/syscall.h              unblocks   1    still blocked:   1
+tgmath.h                   unblocks   1    still blocked:   0
```

**Does the ordering diverge from an interface count?** This is the
question the OPTS work found most decision-useful, and it was worth
checking here rather than assuming it repeats. Over the eight absent
headers for which `test/POSIX-GAP-ACCOUNTING.md` publishes an absent-
interface count ("XSI vs base, restated"), the three rankings are:

| rank | by corpus weight (tests naming it) | by interface count (absences) | by tests per interface |
|---|---|---|---|
| 1 | `pthread.h (23)` | `pthread.h (70)` | `langinfo.h (1.500)` |
| 2 | `semaphore.h (9)` | `semaphore.h (10)` | `semaphore.h (0.900)` |
| 3 | `langinfo.h (3)` | `sys/msg.h (4)` | `iconv.h (0.667)` |
| 4 | `iconv.h (2)` | `sys/shm.h (4)` | `sys/mman.h (0.667)` |
| 5 | `sys/mman.h (2)` | `iconv.h (3)` | `sys/sem.h (0.333)` |
| 6 | `sys/msg.h (1)` | `sys/mman.h (3)` | `pthread.h (0.329)` |
| 7 | `sys/sem.h (1)` | `sys/sem.h (3)` | `sys/msg.h (0.250)` |
| 8 | `sys/shm.h (1)` | `langinfo.h (2)` | `sys/shm.h (0.250)` |

**It diverges, and more sharply than the prose version of this argument
would have guessed.** `pthread.h` leads by corpus weight and by
interface count alike — that much any measure agrees on, and it is the
one place the two orders coincide. Everywhere else they come apart, and
the extreme case is `pthread.h` itself: **first by both raw measures,
sixth of eight by tests-per-interface.** 70 absent interfaces standing
in front of 23 tests is 0.33 tests per interface, against
`langinfo.h`'s 1.50 — `nl_langinfo` and `nl_langinfo_l`, two
declarations, blocking three tests. Ranked by density `langinfo.h`
beats `pthread.h` by a factor of 4.6, and `semaphore.h` beats it by
2.7.

By interface count `semaphore.h` (10) outranks `langinfo.h` (2) five
to one; by density `langinfo.h` wins. `sys/msg.h` and `sys/shm.h`
sit third and fourth on interface count and last on density. The two
rankings are not the same ranking, and the difference is not noise:
counting *declarations* weights a header by how much of it there is,
counting *tests* weights it by how much of the corpus stands behind it,
and only the second reflects what the suite actually exercises.

**And yet the honest conclusion is still "build `pthread.h`", which is
worth saying out loud.** Density is the right tiebreak between headers
of comparable reach; it is not a work list on its own. `langinfo.h` at
1.50 tests per interface unblocks **3 tests**. `pthread.h` at 0.33
unblocks **16**, and 23 stop naming an absent header. A report that
handed over the density column as a priority order would be
recommending two days of work for 3 tests over the one header that moves
39% of the blocked population.

**The divergence is also weaker here than in OPTS, and that is itself a
finding.** OPTS ranked `aio.h` above `sys/mman.h` by an order of
magnitude across 1610 tests. This corpus has 146, of which 41 are
header-blocked, and its long tail is **eleven headers blocking exactly
one test each** — at that granularity the difference between rank 7 and
rank 12 is one test, which is to say it is nothing. **`pthread.h` is
the only lever this corpus can rank with confidence**, and that reading
is not available from the closure or the interface count alone; it takes
both, disagreeing, to see that the rest of the table is under-powered.

**Caveat, stated because the number would otherwise be over-read.** A
header appearing does not mean those tests would then *pass*, or even
*link* — they would still need the functions behind it, and this suite
would then start reporting on their behaviour. The closure measures **how
much of the corpus becomes reachable**, which is the right question for
prioritising a header and the wrong one for predicting a pass rate.

## 4. Class B, itemised

The class the ledger cannot see. Every row here is a test whose
`#include` **succeeded**.

| sub-class | tests | symbol / key | what it is |
|---|---|---|---|
| **declared-undefined-ok** | 1 | `daemon`  | declared in a public header and marked undefined-ok: deliberately unimplemented |
| **deleted-on-purpose** | 1 | `clearenv`  | removed on purpose in 49b8099 -- env: drop secure_getenv() and clearenv() |
| **harness-companion** | 1 | `t`  | provided by tls_align_dso.so, which this driver does not build |
| **harness-dso** | 4 | `main`  | a shared object, not a program: upstream builds this with -shared (Makefile:135) |
| **harness-rpath** | 3 | `__rpath`  | this tree's dlopen requires the program to define __rpath (include/ntlibc/rpath.h) |
| **macro-absent** | 1 | `SOCK_CLOEXEC`  | header resolves; the constant does not |
| **never-implemented** | 5 | `fgetwc` `fwscanf` `wcsstr` `wcstol` `wcstoul`  | never declared and never defined here |
| **no-interposition** | 1 | `malloc`  | the test defines its own malloc; this library's archive does not allow overriding it |

Per test:

| test | sub-class | symbol(s) |
|---|---|---|
| `daemon-failure` | declared-undefined-ok | `daemon` |
| `dlopen_dso` | harness-dso | `main` |
| `dlopen` | harness-rpath | `__rpath` |
| `env` | deleted-on-purpose | `clearenv` |
| `fgetwc-buffering` | never-implemented | `fgetwc` |
| `flockfile-list` | no-interposition | `malloc` |
| `fwscanf` | never-implemented | `fgetwc fwscanf` |
| `socket` | macro-absent | `SOCK_CLOEXEC` |
| `tls_align_dlopen` | harness-rpath | `__rpath` |
| `tls_align_dso` | harness-dso | `main` |
| `tls_align` | harness-companion | `t` |
| `tls_get_new-dtv_dso` | harness-dso | `main` |
| `tls_init_dlopen` | harness-rpath | `__rpath` |
| `tls_init_dso` | harness-dso | `main` |
| `wcsstr-false-negative` | never-implemented | `wcsstr` |
| `wcsstr` | never-implemented | `wcsstr` |
| `wcstol` | never-implemented | `wcstol wcstoul` |

### 4a. Not a library gap at all

**8 of the 17 class-B tests are not evidence of a missing
interface**, and this is the single most important correction the map
makes to the gate's bare count:

- **`harness-dso` / `harness-companion`** — `*_dso.c` files are
  *shared objects*. Upstream's own `Makefile:135-136` builds them with
  `-shared`; they have no `main()` and were never standalone programs.
  `tools/libc-test.sh` compiles every `.c` in the corpus as a program,
  so their absent `main()` surfaces as an unbuildable *test*. Nothing
  about this library is missing. `tls_align` is the consuming half:
  it wants `t` out of `tls_align_dso.so`.
- **`harness-rpath`** — a real divergence, but an **ABI-contract** one
  rather than an absent interface. This tree's `dlopen()` is delay-load
  based and, by documented design, the *program* declares its own search
  path: `include/ntlibc/rpath.h` says `__rpath` "is the one thing a
  program using either mechanism still declares itself". Upstream's C has
  never heard of that contract. Filing it under "missing symbol" would
  put a deliberate design decision in the same bucket as `wcsstr`.

Netting those out, the genuine interface gap behind the gate's
unbuildable count is **9 class-B tests**, not 17 — and
that correction is invisible in the gate's summary line.

## 5. How this report stays honest

Four invariants, all checked by `tools/libc-test-map.sh --check`, any of
which failing is a hard error rather than a warning. They exist because
this instrument has a failure mode an ordinary test does not: **if the
measurement silently stops working, the gap appears to close**, and that
is good news, which is the most dangerous shape a bug can take.

| # | invariant | pinned | measured |
|---|---|---|---|
| 1 | **Census** — files discovered under `src/functional` + `src/regression` | 77 + 69 = 146 | 77 + 69 = 146 |
| 2 | **Partition** — `A + B + C` must equal the census exactly | 146 | 40 + 17 + 89 = 146 |
| 3a | **Upper floor** — `C` (builds) ≥ pin. Catches "the compiler stopped finding `libc.a`" | ≥ 80 | 89 |
| 3b | **Lower floor** — `A + B` (blocked) ≥ pin. **The one that matters**: catches an `-I` pointing at a host libc, which makes everything link and the gap read as *zero* | ≥ 40 | 57 |
| 4a | **Canary, blocked** — `pthread_cond-smasher` must still classify A | A | A |
| 4b | **Canary, building** — `string` must still classify C | C | C |

Invariant 3b's pin moves **down** only in a commit that also shows the
header or symbol that closed the gap.

**Why the lower floor is on `A + B` and not on `A` alone.** Measured,
not assumed: adding `-I/usr/include` to the classifier — the exact
accident invariant 3b exists to catch — moves the split from
`A=41 B=17` to `A=36 B=22`. The host's `pthread.h`, `semaphore.h`
and friends resolve, five tests stop failing at `#include` and start
failing at *link*, and **`A + B` does not move at all: 58 either way.**
A floor on `A` alone would have fired on a harmless reclassification
while still missing the case where everything genuinely links. The
partition is what makes the floor mean something.

**Each of these has been demonstrated firing**, by breaking the
condition and watching the check go red — not merely reasoned about,
because a guard nobody has seen fail is a guard nobody knows works:

| # | how it was broken | what it said |
|---|---|---|
| 1 | one `.c` removed from a copy of the corpus | `pinned 77 + 69, found 76 + 69` |
| 2 | one classifier bucket made to return without emitting its row | `A=41 B=16 C=88 sum=145, census=146` |
| 3a | the build step forced to fail for every test | `only 0 tests build, floor is 80` |
| 3b | the driver made to stop registering build failures — the gap reads as **closed** | `only 0 tests blocked, floor is 40` |
| 4 | a canary re-pinned to the wrong class, population untouched | `pthread_cond-smasher is 'A' (want C)` |
| — | deletion pin pointed at a commit that does not remove `clearenv` | `eb5a607 does not remove any line mentioning 'clearenv'` |
| — | recorded SHA replaced with a commit off this history | `not an ancestor of HEAD` |

The 3b run is the one worth reading twice: **invariant 4 fired at the
same time**, because a driver that has stopped seeing build failures
classifies `pthread_cond-smasher` as `C`. That is the design working.
Note also that the *building* canary still agreed in that run — `string`
was `C`, as pinned — which is why there are two canaries pointing in
opposite directions and not one.

Invariant 4 is the specific check that distinguishes *"the gap closed"*
from *"the measurement broke"*: **a closed gap moves the population; a
broken measurement moves the canaries too.** Two canaries rather than
one, in opposite directions, because a classifier that has started
answering constantly — always `A`, or always `C` — satisfies either one
alone.

And one process invariant tooling cannot enforce on its own: the ntlibc
SHA recorded above must be an **ancestor of `HEAD`**, which `--check`
verifies. That is what stops this file being quietly months old while
looking authoritative.

The `deleted-on-purpose` annotations in section 4 are verified the same
way, and for the same reason: `--check` confirms each named commit is an
ancestor of `HEAD` **and** that it actually removed that symbol. A prose
claim nobody checks is how a report starts lying.

## 6. Per-test ledger

All 146 tests, so that a regeneration diffs meaningfully. This
section goes last because nobody reads it directly; it is the raw
material that makes the rest reviewable.

| test | corpus | class | sub-class | key | detail |
|---|---|---|---|---|---|
| `clocale_mbfuncs` | functional | A | header-absent | `langinfo.h` | #include <langinfo.h> does not resolve |
| `crypt` | functional | A | header-absent | `crypt.h` | #include <crypt.h> does not resolve |
| `dn_expand-empty` | regression | A | header-absent | `resolv.h` | #include <resolv.h> does not resolve |
| `dn_expand-ptr-0` | regression | A | header-absent | `resolv.h` | #include <resolv.h> does not resolve |
| `iconv-roundtrips` | regression | A | header-absent | `iconv.h` | #include <iconv.h> does not resolve |
| `iconv_open` | functional | A | header-absent | `iconv.h` | #include <iconv.h> does not resolve |
| `ipc_msg` | functional | A | header-absent | `sys/msg.h` | #include <sys/msg.h> does not resolve |
| `ipc_sem` | functional | A | header-absent | `sys/sem.h` | #include <sys/sem.h> does not resolve |
| `ipc_shm` | functional | A | header-absent | `sys/shm.h` | #include <sys/shm.h> does not resolve |
| `malloc-brk-fail` | regression | A | header-absent | `sys/mman.h` | #include <sys/mman.h> does not resolve |
| `mbc` | functional | A | header-absent | `langinfo.h` | #include <langinfo.h> does not resolve |
| `mntent` | functional | A | header-absent | `mntent.h` | #include <mntent.h> does not resolve |
| `pthread-robust-detach` | regression | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `pthread_atfork-errno-clobber` | regression | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `pthread_cancel` | functional | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `pthread_cancel-points` | functional | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `pthread_cancel-sem_wait` | regression | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `pthread_cond` | functional | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `pthread_cond-smasher` | regression | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `pthread_cond_wait-cancel_ignored` | regression | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `pthread_condattr_setclock` | regression | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `pthread_create-oom` | regression | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `pthread_exit-cancel` | regression | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `pthread_exit-dtor` | regression | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `pthread_mutex` | functional | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `pthread_mutex_pi` | functional | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `pthread_once-deadlock` | regression | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `pthread_robust` | functional | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `pthread_rwlock-ebusy` | regression | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `pthread_tsd` | functional | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `raise-race` | regression | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `sem_close-unmap` | regression | A | header-absent | `semaphore.h` | #include <semaphore.h> does not resolve |
| `sem_init` | functional | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `sem_open` | functional | A | header-absent | `semaphore.h` | #include <semaphore.h> does not resolve |
| `swprintf` | functional | A | header-absent | `langinfo.h` | #include <langinfo.h> does not resolve |
| `syscall-sign-extend` | regression | A | header-absent | `sys/syscall.h` | #include <sys/syscall.h> does not resolve |
| `tgmath` | functional | A | header-absent | `tgmath.h` | #include <tgmath.h> does not resolve |
| `tls_get_new-dtv` | regression | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `tls_init` | functional | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `tls_local_exec` | functional | A | header-absent | `pthread.h` | #include <pthread.h> does not resolve |
| `daemon-failure` | regression | B | declared-undefined-ok | `daemon` | declared in a public header and marked undefined-ok: deliberately unimplemented |
| `env` | functional | B | deleted-on-purpose | `clearenv` | removed on purpose in 49b8099 -- env: drop secure_getenv() and clearenv() |
| `tls_align` | functional | B | harness-companion | `t` | provided by tls_align_dso.so, which this driver does not build |
| `dlopen_dso` | functional | B | harness-dso | `main` | a shared object, not a program: upstream builds this with -shared (Makefile:135) |
| `tls_align_dso` | functional | B | harness-dso | `main` | a shared object, not a program: upstream builds this with -shared (Makefile:135) |
| `tls_get_new-dtv_dso` | regression | B | harness-dso | `main` | a shared object, not a program: upstream builds this with -shared (Makefile:135) |
| `tls_init_dso` | functional | B | harness-dso | `main` | a shared object, not a program: upstream builds this with -shared (Makefile:135) |
| `dlopen` | functional | B | harness-rpath | `__rpath` | this tree's dlopen requires the program to define __rpath (include/ntlibc/rpath.h) |
| `tls_align_dlopen` | functional | B | harness-rpath | `__rpath` | this tree's dlopen requires the program to define __rpath (include/ntlibc/rpath.h) |
| `tls_init_dlopen` | functional | B | harness-rpath | `__rpath` | this tree's dlopen requires the program to define __rpath (include/ntlibc/rpath.h) |
| `socket` | functional | B | macro-absent | `SOCK_CLOEXEC` | header resolves; the constant does not |
| `fgetwc-buffering` | regression | B | never-implemented | `fgetwc` | never declared and never defined here |
| `fwscanf` | functional | B | never-implemented | `fgetwc fwscanf` | never declared and never defined here |
| `wcsstr` | functional | B | never-implemented | `wcsstr` | never declared and never defined here |
| `wcsstr-false-negative` | regression | B | never-implemented | `wcsstr` | never declared and never defined here |
| `wcstol` | functional | B | never-implemented | `wcstol wcstoul` | never declared and never defined here |
| `flockfile-list` | regression | B | no-interposition | `malloc` | the test defines its own malloc; this library's archive does not allow overriding it |
| `argv` | functional | C | builds | `-` |  |
| `basename` | functional | C | builds | `-` |  |
| `clock_gettime` | functional | C | builds | `-` |  |
| `dirname` | functional | C | builds | `-` |  |
| `execle-env` | regression | C | builds | `-` |  |
| `fcntl` | functional | C | builds | `-` |  |
| `fdopen` | functional | C | builds | `-` |  |
| `fflush-exit` | regression | C | builds | `-` |  |
| `fgets-eof` | regression | C | builds | `-` |  |
| `fnmatch` | functional | C | builds | `-` |  |
| `fpclassify-invalid-ld80` | regression | C | builds | `-` |  |
| `fscanf` | functional | C | builds | `-` |  |
| `ftello-unflushed-append` | regression | C | builds | `-` |  |
| `getpwnam_r-crash` | regression | C | builds | `-` |  |
| `getpwnam_r-errno` | regression | C | builds | `-` |  |
| `inet_ntop-v4mapped` | regression | C | builds | `-` |  |
| `inet_pton` | functional | C | builds | `-` |  |
| `inet_pton-empty-last-field` | regression | C | builds | `-` |  |
| `iswspace-null` | regression | C | builds | `-` |  |
| `lrand48-signextend` | regression | C | builds | `-` |  |
| `lseek-large` | regression | C | builds | `-` |  |
| `malloc-0` | regression | C | builds | `-` |  |
| `malloc-oom` | regression | C | builds | `-` |  |
| `mbsrtowcs-overflow` | regression | C | builds | `-` |  |
| `memmem-oob` | regression | C | builds | `-` |  |
| `memmem-oob-read` | regression | C | builds | `-` |  |
| `memstream` | functional | C | builds | `-` |  |
| `mkdtemp-failure` | regression | C | builds | `-` |  |
| `mkstemp-failure` | regression | C | builds | `-` |  |
| `popen` | functional | C | builds | `-` |  |
| `printf-1e9-oob` | regression | C | builds | `-` |  |
| `printf-fmt-g-round` | regression | C | builds | `-` |  |
| `printf-fmt-g-zeros` | regression | C | builds | `-` |  |
| `printf-fmt-n` | regression | C | builds | `-` |  |
| `putenv-doublefree` | regression | C | builds | `-` |  |
| `qsort` | functional | C | builds | `-` |  |
| `random` | functional | C | builds | `-` |  |
| `regex-backref-0` | regression | C | builds | `-` |  |
| `regex-bracket-icase` | regression | C | builds | `-` |  |
| `regex-ere-backref` | regression | C | builds | `-` |  |
| `regex-escaped-high-byte` | regression | C | builds | `-` |  |
| `regex-negated-range` | regression | C | builds | `-` |  |
| `regexec-nosub` | regression | C | builds | `-` |  |
| `rewind-clear-error` | regression | C | builds | `-` |  |
| `rlimit-open-files` | regression | C | builds | `-` |  |
| `scanf-bytes-consumed` | regression | C | builds | `-` |  |
| `scanf-match-literal-eof` | regression | C | builds | `-` |  |
| `scanf-nullbyte-char` | regression | C | builds | `-` |  |
| `search_hsearch` | functional | C | builds | `-` |  |
| `search_insque` | functional | C | builds | `-` |  |
| `search_lsearch` | functional | C | builds | `-` |  |
| `search_tsearch` | functional | C | builds | `-` |  |
| `setenv-oom` | regression | C | builds | `-` |  |
| `setjmp` | functional | C | builds | `-` |  |
| `setvbuf-unget` | regression | C | builds | `-` |  |
| `sigaltstack` | regression | C | builds | `-` |  |
| `sigprocmask-internal` | regression | C | builds | `-` |  |
| `sigreturn` | regression | C | builds | `-` |  |
| `snprintf` | functional | C | builds | `-` |  |
| `spawn` | functional | C | builds | `-` |  |
| `sscanf` | functional | C | builds | `-` |  |
| `sscanf-eof` | regression | C | builds | `-` |  |
| `sscanf_long` | functional | C | builds | `-` |  |
| `stat` | functional | C | builds | `-` |  |
| `statvfs` | regression | C | builds | `-` |  |
| `strftime` | functional | C | builds | `-` |  |
| `string` | functional | C | builds | `-` |  |
| `string_memcpy` | functional | C | builds | `-` |  |
| `string_memmem` | functional | C | builds | `-` |  |
| `string_memset` | functional | C | builds | `-` |  |
| `string_strchr` | functional | C | builds | `-` |  |
| `string_strcspn` | functional | C | builds | `-` |  |
| `string_strstr` | functional | C | builds | `-` |  |
| `strptime` | functional | C | builds | `-` |  |
| `strtod` | functional | C | builds | `-` |  |
| `strtod_long` | functional | C | builds | `-` |  |
| `strtod_simple` | functional | C | builds | `-` |  |
| `strtof` | functional | C | builds | `-` |  |
| `strtol` | functional | C | builds | `-` |  |
| `strtold` | functional | C | builds | `-` |  |
| `strverscmp` | regression | C | builds | `-` |  |
| `time` | functional | C | builds | `-` |  |
| `udiv` | functional | C | builds | `-` |  |
| `ungetc` | functional | C | builds | `-` |  |
| `uselocale-0` | regression | C | builds | `-` |  |
| `utime` | functional | C | builds | `-` |  |
| `vfork` | functional | C | builds | `-` |  |
| `wcsncpy-read-overflow` | regression | C | builds | `-` |  |
| `wordexp` | functional | C | builds | `-` |  |

<!-- BEGIN ntlibc-generated-data v1 -- the rows this report was rendered from.
Do not edit by hand -- see "the data block" in tools/libc-test-map.sh.

s	ntlibc	0d9eed249b7cdfc3af78cbf736d6884a891c0c94
s	libctest	68edb8bd73dab8147ee54c8bec638f4d2b3cff37
s	cc	x86_64-win32-tcc
s	functional	77
s	regression	69
s	tests	146
h	clocale_mbfuncs	langinfo.h
h	crypt	crypt.h
h	dn_expand-empty	resolv.h
h	dn_expand-ptr-0	resolv.h
h	iconv-roundtrips	iconv.h
h	iconv_open	iconv.h
h	ipc_msg	sys/msg.h
h	ipc_sem	sys/sem.h
h	ipc_shm	sys/shm.h
h	malloc-brk-fail	sys/mman.h
h	mbc	langinfo.h
h	mntent	mntent.h
h	pthread-robust-detach	pthread.h
h	pthread_atfork-errno-clobber	pthread.h
h	pthread_cancel	pthread.h
h	pthread_cancel	semaphore.h
h	pthread_cancel-points	pthread.h
h	pthread_cancel-points	semaphore.h
h	pthread_cancel-points	sys/mman.h
h	pthread_cancel-sem_wait	pthread.h
h	pthread_cancel-sem_wait	semaphore.h
h	pthread_cond	pthread.h
h	pthread_cond-smasher	pthread.h
h	pthread_cond_wait-cancel_ignored	pthread.h
h	pthread_condattr_setclock	pthread.h
h	pthread_create-oom	pthread.h
h	pthread_exit-cancel	pthread.h
h	pthread_exit-dtor	pthread.h
h	pthread_mutex	pthread.h
h	pthread_mutex	semaphore.h
h	pthread_mutex_pi	pthread.h
h	pthread_mutex_pi	semaphore.h
h	pthread_once-deadlock	pthread.h
h	pthread_once-deadlock	semaphore.h
h	pthread_robust	pthread.h
h	pthread_rwlock-ebusy	pthread.h
h	pthread_tsd	pthread.h
h	raise-race	pthread.h
h	sem_close-unmap	semaphore.h
h	sem_init	pthread.h
h	sem_init	semaphore.h
h	sem_open	semaphore.h
h	swprintf	langinfo.h
h	syscall-sign-extend	sys/syscall.h
h	tgmath	tgmath.h
h	tls_get_new-dtv	pthread.h
h	tls_init	pthread.h
h	tls_local_exec	pthread.h
t	argv	functional	C	builds		
t	basename	functional	C	builds		
t	clocale_mbfuncs	functional	A	header-absent	langinfo.h	#include <langinfo.h> does not resolve
t	clock_gettime	functional	C	builds		
t	crypt	functional	A	header-absent	crypt.h	#include <crypt.h> does not resolve
t	daemon-failure	regression	B	declared-undefined-ok	daemon	declared in a public header and marked undefined-ok: deliberately unimplemented
t	dirname	functional	C	builds		
t	dlopen	functional	B	harness-rpath	__rpath	this tree's dlopen requires the program to define __rpath (include/ntlibc/rpath.h)
t	dlopen_dso	functional	B	harness-dso	main	a shared object, not a program: upstream builds this with -shared (Makefile:135)
t	dn_expand-empty	regression	A	header-absent	resolv.h	#include <resolv.h> does not resolve
t	dn_expand-ptr-0	regression	A	header-absent	resolv.h	#include <resolv.h> does not resolve
t	env	functional	B	deleted-on-purpose	clearenv	removed on purpose in 49b8099 -- env: drop secure_getenv() and clearenv()
t	execle-env	regression	C	builds		
t	fcntl	functional	C	builds		
t	fdopen	functional	C	builds		
t	fflush-exit	regression	C	builds		
t	fgets-eof	regression	C	builds		
t	fgetwc-buffering	regression	B	never-implemented	fgetwc	never declared and never defined here
t	flockfile-list	regression	B	no-interposition	malloc	the test defines its own malloc; this library's archive does not allow overriding it
t	fnmatch	functional	C	builds		
t	fpclassify-invalid-ld80	regression	C	builds		
t	fscanf	functional	C	builds		
t	ftello-unflushed-append	regression	C	builds		
t	fwscanf	functional	B	never-implemented	fgetwc fwscanf	never declared and never defined here
t	getpwnam_r-crash	regression	C	builds		
t	getpwnam_r-errno	regression	C	builds		
t	iconv-roundtrips	regression	A	header-absent	iconv.h	#include <iconv.h> does not resolve
t	iconv_open	functional	A	header-absent	iconv.h	#include <iconv.h> does not resolve
t	inet_ntop-v4mapped	regression	C	builds		
t	inet_pton	functional	C	builds		
t	inet_pton-empty-last-field	regression	C	builds		
t	ipc_msg	functional	A	header-absent	sys/msg.h	#include <sys/msg.h> does not resolve
t	ipc_sem	functional	A	header-absent	sys/sem.h	#include <sys/sem.h> does not resolve
t	ipc_shm	functional	A	header-absent	sys/shm.h	#include <sys/shm.h> does not resolve
t	iswspace-null	regression	C	builds		
t	lrand48-signextend	regression	C	builds		
t	lseek-large	regression	C	builds		
t	malloc-0	regression	C	builds		
t	malloc-brk-fail	regression	A	header-absent	sys/mman.h	#include <sys/mman.h> does not resolve
t	malloc-oom	regression	C	builds		
t	mbc	functional	A	header-absent	langinfo.h	#include <langinfo.h> does not resolve
t	mbsrtowcs-overflow	regression	C	builds		
t	memmem-oob	regression	C	builds		
t	memmem-oob-read	regression	C	builds		
t	memstream	functional	C	builds		
t	mkdtemp-failure	regression	C	builds		
t	mkstemp-failure	regression	C	builds		
t	mntent	functional	A	header-absent	mntent.h	#include <mntent.h> does not resolve
t	popen	functional	C	builds		
t	printf-1e9-oob	regression	C	builds		
t	printf-fmt-g-round	regression	C	builds		
t	printf-fmt-g-zeros	regression	C	builds		
t	printf-fmt-n	regression	C	builds		
t	pthread-robust-detach	regression	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	pthread_atfork-errno-clobber	regression	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	pthread_cancel	functional	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	pthread_cancel-points	functional	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	pthread_cancel-sem_wait	regression	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	pthread_cond	functional	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	pthread_cond-smasher	regression	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	pthread_cond_wait-cancel_ignored	regression	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	pthread_condattr_setclock	regression	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	pthread_create-oom	regression	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	pthread_exit-cancel	regression	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	pthread_exit-dtor	regression	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	pthread_mutex	functional	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	pthread_mutex_pi	functional	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	pthread_once-deadlock	regression	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	pthread_robust	functional	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	pthread_rwlock-ebusy	regression	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	pthread_tsd	functional	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	putenv-doublefree	regression	C	builds		
t	qsort	functional	C	builds		
t	raise-race	regression	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	random	functional	C	builds		
t	regex-backref-0	regression	C	builds		
t	regex-bracket-icase	regression	C	builds		
t	regex-ere-backref	regression	C	builds		
t	regex-escaped-high-byte	regression	C	builds		
t	regex-negated-range	regression	C	builds		
t	regexec-nosub	regression	C	builds		
t	rewind-clear-error	regression	C	builds		
t	rlimit-open-files	regression	C	builds		
t	scanf-bytes-consumed	regression	C	builds		
t	scanf-match-literal-eof	regression	C	builds		
t	scanf-nullbyte-char	regression	C	builds		
t	search_hsearch	functional	C	builds		
t	search_insque	functional	C	builds		
t	search_lsearch	functional	C	builds		
t	search_tsearch	functional	C	builds		
t	sem_close-unmap	regression	A	header-absent	semaphore.h	#include <semaphore.h> does not resolve
t	sem_init	functional	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	sem_open	functional	A	header-absent	semaphore.h	#include <semaphore.h> does not resolve
t	setenv-oom	regression	C	builds		
t	setjmp	functional	C	builds		
t	setvbuf-unget	regression	C	builds		
t	sigaltstack	regression	C	builds		
t	sigprocmask-internal	regression	C	builds		
t	sigreturn	regression	C	builds		
t	snprintf	functional	C	builds		
t	socket	functional	B	macro-absent	SOCK_CLOEXEC	header resolves; the constant does not
t	spawn	functional	C	builds		
t	sscanf	functional	C	builds		
t	sscanf-eof	regression	C	builds		
t	sscanf_long	functional	C	builds		
t	stat	functional	C	builds		
t	statvfs	regression	C	builds		
t	strftime	functional	C	builds		
t	string	functional	C	builds		
t	string_memcpy	functional	C	builds		
t	string_memmem	functional	C	builds		
t	string_memset	functional	C	builds		
t	string_strchr	functional	C	builds		
t	string_strcspn	functional	C	builds		
t	string_strstr	functional	C	builds		
t	strptime	functional	C	builds		
t	strtod	functional	C	builds		
t	strtod_long	functional	C	builds		
t	strtod_simple	functional	C	builds		
t	strtof	functional	C	builds		
t	strtol	functional	C	builds		
t	strtold	functional	C	builds		
t	strverscmp	regression	C	builds		
t	swprintf	functional	A	header-absent	langinfo.h	#include <langinfo.h> does not resolve
t	syscall-sign-extend	regression	A	header-absent	sys/syscall.h	#include <sys/syscall.h> does not resolve
t	tgmath	functional	A	header-absent	tgmath.h	#include <tgmath.h> does not resolve
t	time	functional	C	builds		
t	tls_align	functional	B	harness-companion	t	provided by tls_align_dso.so, which this driver does not build
t	tls_align_dlopen	functional	B	harness-rpath	__rpath	this tree's dlopen requires the program to define __rpath (include/ntlibc/rpath.h)
t	tls_align_dso	functional	B	harness-dso	main	a shared object, not a program: upstream builds this with -shared (Makefile:135)
t	tls_get_new-dtv	regression	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	tls_get_new-dtv_dso	regression	B	harness-dso	main	a shared object, not a program: upstream builds this with -shared (Makefile:135)
t	tls_init	functional	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	tls_init_dlopen	functional	B	harness-rpath	__rpath	this tree's dlopen requires the program to define __rpath (include/ntlibc/rpath.h)
t	tls_init_dso	functional	B	harness-dso	main	a shared object, not a program: upstream builds this with -shared (Makefile:135)
t	tls_local_exec	functional	A	header-absent	pthread.h	#include <pthread.h> does not resolve
t	udiv	functional	C	builds		
t	ungetc	functional	C	builds		
t	uselocale-0	regression	C	builds		
t	utime	functional	C	builds		
t	vfork	functional	C	builds		
t	wcsncpy-read-overflow	regression	C	builds		
t	wcsstr	functional	B	never-implemented	wcsstr	never declared and never defined here
t	wcsstr-false-negative	regression	B	never-implemented	wcsstr	never declared and never defined here
t	wcstol	functional	B	never-implemented	wcstol wcstoul	never declared and never defined here
t	wordexp	functional	C	builds		
END ntlibc-generated-data -->
